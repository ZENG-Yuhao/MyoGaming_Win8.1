<Page
    x:Class="MyoGaming.GuidePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyoGaming"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="6*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" >
            <TextBlock Text="#color blue# = Direction being pointed out." FontSize="20" Foreground="Black" HorizontalAlignment="Center"/>
            <TextBlock Text="#color red# = Oppisite direction of the direction being pointed out." FontSize="20" Foreground="Black" HorizontalAlignment="Center"/>
            <HyperlinkButton x:Name="btn_back" Content="Back" HorizontalAlignment="Center" FontSize="30" Click="btn_back_Click" />
        </StackPanel>
        <StackPanel Grid.Row="0">
            <Image Source="Assets/guide.png" Stretch="UniformToFill" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
